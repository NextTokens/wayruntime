# Normalize to LF in the repo; keep LF in working copies for files
# whose interpreters choke on CRLF (bash, make).
* text=auto
*.sh text eol=lf
Makefile text eol=lf
*.py text eol=lf
*.ps1 text eol=crlf
